[package]
name = "comparison_common"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared protocol constants for backend comparison benchmarks"

[dependencies]
serde = { workspace = true }
