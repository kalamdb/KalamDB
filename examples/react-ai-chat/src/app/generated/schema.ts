// Generated by kalam schema gen. Do not edit.
// contract_hash: ac966bd5e167f6601d91062e5f61fd5f3f1b1c8b533df5e296ec13f65d1839a8

import type { FileRef } from '@kalamdb/client';
import { kTable, file, type KalamFileUpload } from '@kalamdb/orm';
import { sql } from 'drizzle-orm';
import { text, timestamp, bigint } from 'drizzle-orm/pg-core';

export type JsonValue = string | number | boolean | null | JsonValue[] | { [key: string]: JsonValue };
export type KalamFileColumn = FileRef | File | KalamFileUpload | Blob | null;

export type ReactAiChatApprovalActions = {
  id: bigint;
  approval_id: string;
  conversation_id: string;
  action: string;
  created_at: Date;
};

export type ReactAiChatApprovals = {
  id: string;
  conversation_id: string;
  message_id: string;
  title: string;
  body: string;
  status: string;
  created_at: Date;
  updated_at: Date;
};

export type ReactAiChatConversations = {
  id: string;
  title: string;
  summary: string;
  created_at: Date;
  updated_at: Date;
};

export type ReactAiChatMessages = {
  id: bigint;
  client_id: string | null;
  conversation_id: string;
  reply_to_message_id: string | null;
  role: string;
  body: string;
  status: string;
  attachment: KalamFileColumn;
  approval_id: string | null;
  created_at: Date;
  updated_at: Date;
};

export type ReactAiChatTypingTokens = {
  id: bigint;
  conversation_id: string;
  message_id: string;
  status: string;
  token: string;
  created_at: Date;
};

export type ReactAiChatAgentActions =
  | ({ _table: "react_ai_chat:approval_actions" } & ReactAiChatApprovalActions);
export type ReactAiChatAgentMessages =
  | ({ _table: "react_ai_chat:messages" } & ReactAiChatMessages);

export const reactAiChatApprovalActions = kTable.user("react_ai_chat.approval_actions", {
  id: bigint("id", { mode: "bigint" }).default(sql``).primaryKey(),
  approval_id: text("approval_id").notNull(),
  conversation_id: text("conversation_id").notNull(),
  action: text("action").notNull(),
  created_at: timestamp("created_at", { mode: "date" }).default(sql``).notNull(),
});

export const reactAiChatApprovals = kTable.user("react_ai_chat.approvals", {
  id: text("id").primaryKey(),
  conversation_id: text("conversation_id").notNull(),
  message_id: text("message_id").notNull(),
  title: text("title").notNull(),
  body: text("body").notNull(),
  status: text("status").default(sql``).notNull(),
  created_at: timestamp("created_at", { mode: "date" }).default(sql``).notNull(),
  updated_at: timestamp("updated_at", { mode: "date" }).default(sql``).notNull(),
});

export const reactAiChatConversations = kTable.user("react_ai_chat.conversations", {
  id: text("id").primaryKey(),
  title: text("title").notNull(),
  summary: text("summary").default(sql``).notNull(),
  created_at: timestamp("created_at", { mode: "date" }).default(sql``).notNull(),
  updated_at: timestamp("updated_at", { mode: "date" }).default(sql``).notNull(),
});

export const reactAiChatMessages = kTable.user("react_ai_chat.messages", {
  id: bigint("id", { mode: "bigint" }).default(sql``).primaryKey(),
  client_id: text("client_id"),
  conversation_id: text("conversation_id").notNull(),
  reply_to_message_id: text("reply_to_message_id"),
  role: text("role").notNull(),
  body: text("body").notNull(),
  status: text("status").default(sql``).notNull(),
  attachment: file("attachment"),
  approval_id: text("approval_id"),
  created_at: timestamp("created_at", { mode: "date" }).default(sql``).notNull(),
  updated_at: timestamp("updated_at", { mode: "date" }).default(sql``).notNull(),
});

export const reactAiChatTypingTokens = kTable.stream("react_ai_chat.typing_tokens", {
  id: bigint("id", { mode: "bigint" }).default(sql``).primaryKey(),
  conversation_id: text("conversation_id").notNull(),
  message_id: text("message_id").notNull(),
  status: text("status").notNull(),
  token: text("token").default(sql``).notNull(),
  created_at: timestamp("created_at", { mode: "date" }).default(sql``).notNull(),
});

