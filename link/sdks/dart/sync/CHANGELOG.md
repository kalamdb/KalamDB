## 0.6.0-rc.0

- Local-first Flutter runtime with a Drift cache, durable checkpoints, row sync
  state, bidirectional tables, backend-authoritative replicas, custom actions,
  retries, lifecycle handling, and the shared `kalam_link` transport.
