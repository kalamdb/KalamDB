name: kalam_sync_generator_example
description: Canonical chat action module for kalam_sync_generator and the Flutter sync example.
publish_to: none
version: 0.5.6-rc.0

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  kalam_sync:
    path: ../../sync

dev_dependencies:
  build_runner: ^2.16.0
  kalam_sync_generator:
    path: ..

