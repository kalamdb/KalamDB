[package]
name = "quickstart"
version = "0.0.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
kalam-client = { path = "../..", default-features = false, features = ["native-sdk"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
