[package]
name = "file-attachments"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
kalam-client = { path = "../..", default-features = false, features = ["native-sdk", "file-uploads"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
