-- Development fixtures. Keep this file idempotent.
-- `kalam up` / `kalam dev` load it once per database. `kalam db seed` reruns it.
-- Production-purpose environments never load this file automatically.
