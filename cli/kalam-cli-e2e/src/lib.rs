//! Feature-gated optional crates for integration tests (`oidc`, `cloud-aws`).
//!
//! Cargo forbids optional `[dev-dependencies]`, so this package is a tiny lib
//! whose only job is to own those features.
